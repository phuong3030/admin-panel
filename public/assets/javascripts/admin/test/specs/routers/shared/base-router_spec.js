define(['app', 'routers/shared/base-router'], function (App, BaseRouter) {

  describe('Base router', function () {

    var baseRouter;

/*
 *    beforeEach(function () {
 *    
 *      baseRouter = new BaseRouter();
 *    });
 *  
 *    it('should be called start sub application before routing', function () {
 *    
 *      baseRouter._appName = 'Room';
 *      spyOn(App, 'startSubApp');
 *      // Fake call when route changed
 *      baseRouter.before();
 *
 *      expect(App.startSubApp).toHaveBeenCalledWith('Room');
 *    });
 *
 *    it('should be triggered change heading event when route changed', function () {
 *
 *      spyOn(App.vent, 'trigger');
 *      baseRouter.after('!room-list');
 *
 *      expect(App.vent.trigger).toHaveBeenCalledWith('changeHeading', '!room-list');
 *    });
 */
  });
});
